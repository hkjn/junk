// Package junk is as advertised nothing useful.
//
// It exists to debug and test some things out.
package junk

import "fmt"

var Number = 5

func Smell() {
	fmt.Printf("Yuck, junk's smell level is %d\n.", Number)
}
